export interface appSetting {
    isLoading?: boolean
    menubarWidth: number
    menubarHide: boolean
}

export interface locale {
    msgKey: string
    ko_text: string
    jp_text: string
    en_text: string
    added?: boolean
}
