export interface appSetting {
    isLoading?: boolean
    menubarWidth: number
    menubarHide: boolean
}

export interface locale {
    msg_key: string
    lang_type: 'KR' | 'JP' | 'EN'
    trtext: string
}
